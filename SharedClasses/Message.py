class Message:
    def __init__(self, message_id, from_username, to_username, content):
        self.message_id = message_id
        self.from_username = from_username
        self.to_username = to_username
        self.content = content
